import { ReactNode } from "react";
import { Field as FieldData } from "@/type/FieldType";
import "./field.css";

export const Field = ({ data, children }: { data: FieldData, children?: ReactNode }) => {
  return (
    <>Field</>
  );
};
